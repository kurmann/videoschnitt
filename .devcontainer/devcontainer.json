{
    "name": "Python 3",
    "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bullseye",

    // Installiere Poetry und führe dann den restlichen Installationsprozess aus
    "postCreateCommand": "curl -sSL https://install.python-poetry.org | python3 - && poetry export -f requirements.txt --output requirements.txt --without-hashes && echo '-e .' >> requirements.txt && pip install -r requirements.txt",

    "customizations": {
        "vscode": {
            "extensions": [
                "GitHub.copilot",
                "ms-python.python",
                "ms-python.vscode-pylance",
                "tamasfe.even-better-toml"
            ]
        }
    }
}
