services:
  videoschnitt-kraftwerk:
    build: .
    container_name: videoschnitt-kraftwerk
    volumes:
      - logs:/app/logs
    env_file:
      - .env
    environment:
      - ASPNETCORE_ENVIRONMENT=Production

volumes:
  logs: